<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.myvelux.myvelux">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FinalChoiceActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClientActivity" />
        <activity android:name=".RoomActivity" />
        <activity android:name=".ActionActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RoomActivity" />
        </activity>
        <activity android:name=".RangeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ActionActivity" />
        </activity>
        <activity android:name=".TypeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".RangeActivity" />
        </activity>
        <activity android:name=".VersionActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TypeActivity" />
        </activity>
        <activity android:name=".SizeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".VersionActivity" />
        </activity>
        <activity android:name=".FittingActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".SizeActivity" />
        </activity>
        <activity android:name=".MyVeluxPDF">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".FinalChoiceActivity" />
        </activity>
        <activity android:name=".ShowCommandeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".FinalChoiceActivity" />
        </activity>
        <activity android:name=".ShowClientActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".ManageClient"></activity>
    </application>

</manifest>